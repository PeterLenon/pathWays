# Build output — rebuilt inside the container
.next
out

# Dependencies — reinstalled inside the container
node_modules

# Environment files — secrets must not be baked into the image layer cache.
# Pass them via docker-compose `environment:` or `env_file:` at runtime.
.env
.env.*

# Dev tooling
.git
.gitignore
*.md

# macOS
.DS_Store

# Editor
.vscode
.idea
